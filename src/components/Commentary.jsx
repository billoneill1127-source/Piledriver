// Commentary.jsx
// React component — scrolling commentary feed shown during the match.

export default function Commentary({ lines = [] }) {
  return (
    <div className="commentary">
      {/* TODO: render commentary lines, auto-scroll to latest */}
    </div>
  );
}
