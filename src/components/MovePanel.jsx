// MovePanel.jsx
// React component — displays the active wrestler's available moves as buttons.

export default function MovePanel({ moves = [], onSelect }) {
  return (
    <div className="move-panel">
      {/* TODO: render move buttons, disabled when not player's turn */}
    </div>
  );
}
