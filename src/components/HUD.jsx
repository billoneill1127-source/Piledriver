// HUD.jsx
// React component — heads-up display showing health, stamina, and momentum
// bars for both wrestlers.

export default function HUD({ player, opponent }) {
  return (
    <div className="hud">
      {/* TODO: render health/stamina/momentum bars */}
    </div>
  );
}
