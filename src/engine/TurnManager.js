// TurnManager.js
// Manages turn order, phase transitions (player → AI → resolution),
// and fires events consumed by both Phaser scenes and React components.

export class TurnManager {
  constructor() {
    // TODO: initialise turn queue, phase state, event emitter
  }
}
