// WrestlerSelect.js
// Phaser Scene — wrestler selection screen before a match.

import Phaser from 'phaser';

export default class WrestlerSelect extends Phaser.Scene {
  constructor() {
    super({ key: 'WrestlerSelect' });
  }

  preload() {}

  create() {
    // TODO: render wrestler cards, wait for player selection, then start MatchScreen
  }
}
